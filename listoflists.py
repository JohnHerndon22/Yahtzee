# listoflists.py


list_of_lists = [['a', 25, 69, 'Apple'],
                  [5, 'doll', 854, 41.2],
                  [8, 6, 'car', True]]

# del list_of_lists[1]

# print(list_of_lists[0][0])
for list in list_of_lists:
    print(list[0])


ct_element = list()
    for ct in currentTurn:
        ct_element = ct_element.append(ct[0])
    dfcurrentTurn = pd.DataFrame(ct_element,columns=['roll')
    dfcurrentTurn = dfcurrentTurn.sort_values(by=['roll'])


